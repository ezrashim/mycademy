%h1= @lesson.title
%fr-view= @lesson.content.html_safe

- if user_signed_in? && !@enrollment.nil? && @enrollment.leader?
  %li= link_to 'Delete Lesson', course_lesson_path(@course, @lesson), method: :delete
